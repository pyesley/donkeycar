ultralytics>=8.3.0
opencv-python>=4.8
# torch is intentionally not pinned — install the build that matches
# your CUDA version. For an RTX 5090 (Blackwell, sm_120):
#   pip install --pre torch torchvision --index-url https://download.pytorch.org/whl/nightly/cu128
